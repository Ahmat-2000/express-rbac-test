# .env file

```bash
PORT=3001

DB_URI="mongodb+srv://<username>:<password>@cluster0.wk371eo.mongodb.net/Rbac?retryWrites=true&w=majority&appName=Cluster0"

JWT_SECRET="secret-key"
JWT_EXPIRES_IN="24h"

```
